import {topbar,btn,esc} from "../core/utils.js";
export function view(store){const c=store.contacts[0],msgs=store.messages.filter(m=>m.contactId===c.id);return topbar("Shared Inbox","Conversations, automation controls and agent actions",btn("＋ Add Contact","btn primary"))+
`<div class="inbox"><aside class="chatlist"><div class="chatsearch"><input class="input" placeholder="Search conversations…"></div>${store.contacts.map((x,i)=>`<div class="chat ${i===0?"active":""}"><b>${esc(x.name)}</b><p>${esc(store.messages.find(m=>m.contactId===x.id)?.text||"No messages")}</p></div>`).join("")}</aside>
<section style="display:flex;flex-direction:column;min-width:0"><div class="messages" style="flex:1">${msgs.map(m=>`<div class="bubble ${m.dir==="out"?"out":"in"}">${esc(m.text)}<div style="opacity:.5;font-size:8px">${m.time}</div></div>`).join("")}</div><form class="composer" id="composer"><input class="input" name="text" placeholder="Type a message…"><button class="btn">＋</button><button class="btn primary">Send</button></form></section>
<aside class="profile"><div class="profileblock"><b>Customer Snapshot</b><p class="muted">${esc(c.name)} · ${esc(c.phone)}</p></div><div class="profileblock"><label class="muted">LABELS</label><p>${c.labels.map(x=>`<span class="chip">${esc(x)}</span>`).join("")}</p><button class="btn">＋ Add Label</button></div><div class="profileblock"><label class="muted">BOT / AI</label><p>● Bot Active</p><button class="btn">Pause Bot</button> <button class="btn">Pause AI</button></div><div class="profileblock"><label class="muted">ACTIONS</label><p><button class="btn">Schedule</button> <button class="btn">Google Meet</button></p><p><button class="btn">Assign Agent</button> <button class="btn">Add Note</button></p><p><button class="btn">Add Sequence</button> <button class="btn">Start Flow</button></p></div><div class="profileblock"><label class="muted">CUSTOM FIELDS</label><p class="muted">order_id = GM10245<br>city = Chennai<br>membership = VIP</p></div></aside></div>`}
