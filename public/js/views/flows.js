import {topbar,btn} from "../core/utils.js";
export function view(){return topbar("WhatsApp Flows","Multi-screen forms, conditions, fields and JSON import",btn("Import Flow JSON","btn")+" "+btn("＋ New Screen","btn primary"))+
`<div class="flow-layout"><aside class="flow-list"><button class="nodebtn">＋ Add Screen</button>${["Welcome","Customer Details","Order Details","Confirmation"].map((x,i)=>`<div class="flow-screen ${i===1?"active":""}"><b>${i+1}. ${x}</b><p class="muted">${i===1?"Text + Email + Phone":"Form elements and actions"}</p></div>`).join("")}<button class="nodebtn">⇄ Connect Screens</button><button class="nodebtn">{} Edit JSON</button></aside><main style="overflow:auto"><div class="preview-phone"><div class="preview-head">GAMAV · Customer Form</div><div class="muted">Screen 2 / 4</div><div class="preview-field">Name · Text Input</div><div class="preview-field">Email · Email Input</div><div class="preview-field">Upload ID · File Picker</div><div class="preview-field">Condition → membership == VIP</div><div class="preview-field">THEN → VIP · ELSE → Standard</div><div class="preview-button">Next</div></div></main></div>`}
